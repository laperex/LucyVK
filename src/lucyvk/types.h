namespace lucyvk {
	struct Instance;

	struct debugger;

	struct physicaldevice;
	struct LogicalDevice;
	
	struct uniformbuffer;
	struct vertexbuffer;
	struct indexbuffer;

	struct texturesampler;

	struct pipeline;

	struct shader;
}
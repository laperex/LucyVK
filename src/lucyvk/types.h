namespace lucy {
	struct instance;

	struct debugger;

	struct physicaldevice;
	struct logicaldevice;
	
	struct uniformbuffer;
	struct vertexbuffer;
	struct indexbuffer;

	struct texturesampler;

	struct pipeline;

	struct shader;
}